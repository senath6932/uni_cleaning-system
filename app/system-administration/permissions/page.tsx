import { redirect } from "next/navigation";
import { AppShell } from "@/app/_components/app-shell";
import { getAuthContext } from "@/lib/auth";
export default async function PermissionsPage() { const { sessionUser, appUser } = await getAuthContext(); if (!sessionUser) redirect("/login"); if (!appUser || appUser.role !== "GAA") return <main className="p-8">Access denied.</main>; const rows = [["GAA", "View/Create/Edit", "View/Create/Edit", "View/Create/Edit", "View/Create/Edit", "View/Finalize"], ["ADMINISTRATION_OFFICER", "View", "-", "-", "Approve/Reject/Correction", "View"], ["EVALUATING_OFFICER", "View assigned", "-", "Daily monitoring", "-", "-"], ["PHI", "View assigned", "-", "-", "-", "-"], ["VICE_CHANCELLOR", "View", "-", "-", "Future workflow", "-" ]]; return <AppShell user={appUser}><div className="mx-auto max-w-6xl"><p className="eyebrow">System Administration</p><h1 className="mt-2 text-3xl font-semibold">Permission Verification</h1><div className="mt-6 overflow-x-auto rounded-3xl border border-slate-200 bg-white"><table className="min-w-full"><thead><tr>{["Role", "Users", "Reports", "Monitoring", "Admin Review", "Final Reports"].map((h) => <th className="th" key={h}>{h}</th>)}</tr></thead><tbody>{rows.map((row) => <tr className="border-t" key={row[0]}>{row.map((value, index) => <td className="td" key={`${row[0]}-${index}`}>{value}</td>)}</tr>)}</tbody></table></div></div></AppShell>; }
