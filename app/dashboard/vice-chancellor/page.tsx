import Link from "next/link";
import { redirect } from "next/navigation";
import { AppShell } from "@/app/_components/app-shell";
import { getAuthContext } from "@/lib/auth";
import { getViceChancellorDashboardData } from "@/lib/role-dashboards";
import { prisma } from "@/lib/prisma";
export default async function ViceChancellorDashboardPage() { const { sessionUser, appUser } = await getAuthContext(); if (!sessionUser) redirect("/login"); if (!appUser || appUser.role !== "VICE_CHANCELLOR") return <main className="flex min-h-screen items-center justify-center bg-slate-50 p-6"><div className="rounded-3xl border border-red-200 bg-red-50 p-8"><h1 className="text-2xl font-semibold text-red-950">Access Denied</h1><p className="mt-2 text-sm text-red-800">You do not have permission to access this dashboard.</p></div></main>; const data = await getViceChancellorDashboardData(prisma); return <AppShell user={appUser}><div className="mx-auto flex w-full max-w-7xl flex-col gap-6"><header><p className="text-sm font-semibold uppercase tracking-[0.3em] text-cyan-700">Vice Chancellor</p><h1 className="mt-2 text-3xl font-semibold text-slate-950">Vice Chancellor Dashboard</h1><p className="mt-2 text-sm text-slate-600">Cleaning Service Approval Overview</p></header><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Stat title="Pending VC Approval" value={data.pending} /><Stat title="Approved" value={data.approved} /><Stat title="Rejected" value={data.rejected} /><Stat title="Clarification Requested" value={data.clarification} /></section><section className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><h2 className="text-lg font-semibold text-slate-950">Approval workflow</h2><p className="mt-2 text-sm leading-7 text-slate-600">VC approval actions are not yet enabled. This dashboard shows only report status information currently available in the system.</p><Link href="/monthly-reports" className="mt-5 inline-flex rounded-2xl border border-slate-300 px-4 py-2.5 text-sm font-semibold text-slate-700">View Monthly Reports</Link></section></div></AppShell>; }
function Stat({ title, value }: { title: string; value: number }) { return <article className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm"><p className="text-sm font-medium text-slate-500">{title}</p><p className="mt-2 text-3xl font-semibold text-slate-950">{value}</p></article>; }
