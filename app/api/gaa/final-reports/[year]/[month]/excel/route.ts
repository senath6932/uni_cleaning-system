import { NextResponse } from "next/server";
import { requireRole } from "@/lib/auth";
import { getFinalReport, PaymentRecommendationError } from "@/lib/final-reports";
import { generateFinalExcel } from "@/lib/final-report-files";
import { prisma } from "@/lib/prisma";
export async function GET(_: Request, { params }: { params: Promise<{ year: string; month: string }> }) { try { const { appUser } = await requireRole("GAA"); const values = await params; const report = await getFinalReport(prisma, appUser, Number(values.year), Number(values.month)); const file = await generateFinalExcel(report); await prisma.activityLog.create({ data: { userId: appUser.id, action: "FINAL_REPORT_EXCEL_GENERATED", entityType: "FinalReport", entityId: `${values.year}-${values.month}`, description: "Final report Excel workbook generated.", metadata: { year: Number(values.year), month: Number(values.month) } } }); return new NextResponse(file, { headers: { "Content-Type": "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet", "Content-Disposition": `attachment; filename="University_Cleaning_Final_All_Locations_Report_${values.year}_${String(values.month).padStart(2, "0")}.xlsx"` } }); } catch (error) { return NextResponse.json({ error: error instanceof Error ? error.message : "Unable to generate Excel workbook." }, { status: error instanceof PaymentRecommendationError ? error.status : 500 }); } }
